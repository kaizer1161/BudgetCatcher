<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pushertest.www.budgetcatcher">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".BudgetCatcher"
        android:theme="@style/AppTheme">

        <activity
            android:name=".View.Activity.SignInSignUp"
            android:label="@string/signin_up"
            android:screenOrientation="portrait">
        </activity>

        <activity
            android:name=".View.Activity.SignIn"
            android:label="@string/sign_in"
            android:parentActivityName=".View.Activity.SignInSignUp"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".View.Activity.SignUp"
            android:label="@string/sign_up"
            android:parentActivityName=".View.Activity.SignInSignUp"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".View.Activity.ProfileSetup"
            android:label="@string/profile_setup"
            android:parentActivityName=".View.Activity.SignUp"
            android:screenOrientation="portrait"></activity>

        <activity
            android:name=".View.Activity.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".View.Activity.SplashScreen"
            android:label="@string/app_name"
            android:parentActivityName=".View.Activity.SignInSignUp"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">

            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->

        </activity>

    </application>

</manifest>